# Project Notes

- **Formatter:** This project uses [ruff](https://github.com/astral-sh/ruff) for Python code formatting.

## Testing
- **Unit Tests:** Run unit tests using `python3 test_augment_notes.py`.
- **Mocking:** The tests use `unittest.mock` to mock AnkiConnect and Gemini API calls, so no live Anki instance or API key is required for running tests.